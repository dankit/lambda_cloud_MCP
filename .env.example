# Lambda Cloud API key from https://cloud.lambda.ai/api-keys
LAMBDA_API_KEY=

# Local path to the private key (.pem) matching a public key registered in Lambda (used in suggested SSH command only)
LAMBDA_SSH_PEM_PATH=
